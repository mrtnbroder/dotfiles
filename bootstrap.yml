- name: macOS bootstrap
  hosts: local
  # pre_tasks:
  #   - name: Pre-Tasks Echo
  #     debug:
  #       msg: "{{ example_server }}"
  roles:
    - role: macos
      tags: macos
    - role: brew
      tags: brew
    - role: mas
      tags: mas
    - role: npm
      tags: npm
    - role: symlink
      tags: symlink
