[color]
  ui = auto
  interactive = auto
[credential]
  helper = osxkeychain
[diff]
  tool = mvimdiff
[commit]
  template = ~/.gitmessage
[push]
  default = current
[include]
  path = .gitconfig.user
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
